fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
python-dotenv
pydantic
gunicorn
# bcrypt MUST stay at 4.0.1: passlib 1.7.4 is incompatible with bcrypt>=4.1
# (removed __about__, raises ValueError instead of truncating >72 bytes)
# -> crashes worker boot: "password cannot be longer than 72 bytes"
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]

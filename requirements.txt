fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
python-dotenv
pydantic
gunicorn
# bcrypt is ONLY used to verify legacy $2b$ password hashes (auth.py wraps it
# in try/except; server boots fine even if this import fails). Keep pinned:
# bcrypt>=4.1 breaks passlib-based setups and we don't want resolution drift.
bcrypt==4.0.1
python-jose[cryptography]

# AI assistant: HTTP client for LLM providers + PowerPoint export
httpx
python-pptx
